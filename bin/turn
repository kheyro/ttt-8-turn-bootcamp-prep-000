#!/usr/bin/env ruby
require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
move = false

puts "Welcome to Tic Tac Toe!"
display_board(board)

while move == false
  "Please enter 1-9:"
  index = gets.strip
  if turn(index) == 1
  else
  end
end
